$('#<%= @item.id %>').remove();
$('#cart_update').html('<%= number_to_currency current_order.total_price %>');
$('#total_update').html('<%= link_to "Cart | Total items: #{ cart_items }", cart_path %>');
